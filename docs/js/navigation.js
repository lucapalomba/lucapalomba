document.addEventListener('DOMContentLoaded', () => { const pages = [ { name: 'index', url: 'index.html', check: (path) => path.endsWith('index.html') || path.endsWith('/') || path.endsWith('lucapalomba/') }, { name: 'experiences', url: 'experiences.html', check: (path) => path.endsWith('experiences.html') }, { name: 'technologies', url: 'technologies.html', check: (path) => path.endsWith('technologies.html') }, { name: 'contact', url: 'contact.html', check: (path) => path.endsWith('contact.html') } ]; const path = window.location.pathname; const currentPageIndex = pages.findIndex(page => page.check(path)); document.addEventListener('keydown', (e) => { if (currentPageIndex === -1) return; if (e.key === 'ArrowRight') { const nextIndex = (currentPageIndex + 1) % pages.length; navigateTo(pages[nextIndex].url); } else if (e.key === 'ArrowLeft') { const prevIndex = (currentPageIndex - 1 + pages.length) % pages.length; navigateTo(pages[prevIndex].url); } }); let touchStartX = 0; let touchEndX = 0; let touchStartY = 0; let touchEndY = 0; const minSwipeDistance = 100; document.addEventListener('touchstart', (e) => { touchStartX = e.changedTouches[0].screenX; touchStartY = e.changedTouches[0].screenY; }, { passive: true }); document.addEventListener('touchend', (e) => { touchEndX = e.changedTouches[0].screenX; touchEndY = e.changedTouches[0].screenY; handleSwipe(); }, { passive: true }); function handleSwipe() { if (currentPageIndex === -1) return; const distanceX = touchEndX - touchStartX; const distanceY = touchEndY - touchStartY; if (Math.abs(distanceX) < minSwipeDistance) return; if (Math.abs(distanceX) < Math.abs(distanceY) * 1.5) return; if (distanceX > 0) { const prevIndex = (currentPageIndex - 1 + pages.length) % pages.length; navigateTo(pages[prevIndex].url); } else { const nextIndex = (currentPageIndex + 1) % pages.length; navigateTo(pages[nextIndex].url); } } function navigateTo(url) { window.location.href = url; } const navHint = document.getElementById('navigation-hint'); if (navHint) { const isTouch = 'ontouchstart' in window || navigator.maxTouchPoints > 0; if (isTouch) { navHint.innerHTML = 'Swipe <kbd>←</kbd> <kbd>→</kbd> to navigate'; } else { navHint.innerHTML = 'Use keyboard arrows <kbd>←</kbd> <kbd>→</kbd> to navigate'; } navHint.setAttribute('aria-hidden', 'false'); setTimeout(() => { navHint.classList.add('fade-out'); setTimeout(() => { navHint.remove(); }, 1000); }, 5000); } });